/**
 * jwt.js — DEPRECATED.
 *
 * This file is no longer used. Authentication is now handled by Supabase Auth.
 * Supabase issues and verifies all JWTs. The server verifies tokens via
 * supabase.auth.getUser(token) in auth.middleware.js.
 *
 * SAFE TO DELETE once you confirm no other file imports from here.
 * Run: grep -r "utils/jwt" server/src  to verify zero imports.
 */

module.exports = {};
